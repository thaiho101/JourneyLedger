name: Deploy JourneyLedger

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on:
      - self-hosted
      - Linux
      - ARM

    steps:
      - name: Test deploy workflow pickup
        run: |
          echo "Deploy workflow picked up"
          hostname